# Example .env file for local development
# Copy this to `.env` when you want to use the included SQLite dev DB

# Prisma connection string. The default dev DB is stored at `prisma/dev.db`.
DATABASE_URL="file:./prisma/dev.db"

# Optional: configure any other env variables your environment or tooling needs.
# The application stores relay configuration under the user's config directory:
# ~/.config/nostrtrac/relays.json
